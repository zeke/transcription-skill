#!/bin/sh
# script/lint: syntax-check the CLI. No linter dependency; the CLI is
# stdlib-only and kept small enough that py_compile catches real mistakes.
set -e
cd "$(dirname "$0")/.."
python3 -m py_compile skills/transcription-skill/scripts/transcribe.py
echo "lint ok"
